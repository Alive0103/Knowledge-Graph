# 知识图谱实体链接系统 - 完整使用指南

## 📋 目录

1. [环境准备](#环境准备)
2. [数据准备](#数据准备)
3. [模型训练](#模型训练)
4. [实体提取与向量化](#实体提取与向量化)
5. [数据入库](#数据入库)
6. [检索系统使用](#检索系统使用)
7. [完整流程（自动化）](#完整流程自动化)

---

## 🔧 环境准备

### 1. Python环境

确保Python版本 >= 3.8：

```bash
python --version  # 应显示 Python 3.8 或更高版本
```

### 2. 安装依赖

```bash
cd work_wyy
pip install torch transformers zhipuai elasticsearch pandas tqdm numpy
```

或者如果有requirements.txt：

```bash
pip install -r requirements.txt
```

### 3. 下载基础模型

基础模型：`chinese-roberta-wwm-ext-large`

**方法1：手动下载**
- 从HuggingFace下载：https://huggingface.co/hfl/chinese-roberta-wwm-ext-large
- 放置到：`work_wyy/model/chinese-roberta-wwm-ext-large/`

**方法2：使用下载脚本**（如果有）
```bash
python download_models.py
```

### 4. 配置Elasticsearch

确保Elasticsearch服务已启动，并配置连接信息：

编辑 `es_client.py`：
```python
ES_URL = "http://your-es-host:9200"
ES_USERNAME = "your-username"
ES_PASSWORD = "your-password"
```

### 5. 配置智谱AI API（用于LLM重排序）

在 `search_vllm.py` 中配置API Key：
```python
client = ZhipuAI(api_key="your-api-key")
```

---

## 📊 数据准备

### 数据目录结构

确保以下数据目录存在：

```
work_wyy/data/
├── traindata/                    # NER训练数据
│   ├── 1_ner_train.json
│   ├── 1_ner_dev.json
│   └── ... (共6组训练/验证文件)
│
├── ccks_ner/                     # CCKS数据集
│   └── militray/
│       └── PreModel_Encoder_CRF/
│           ├── ccks_8_data_v2/
│           │   ├── train/        # 400个JSON文件
│           │   └── validate_data.json
│           └── data/
│               └── fold0-4/      # 5个fold的训练数据
│
├── nlp_datasets/                 # MSRA通用NER数据
│   └── ner/
│       └── msra/
│           ├── msra_train_bio.txt
│           └── msra_test_bio.txt
│
├── train.txt                     # JSONL格式训练数据（可选）
├── zh_wiki_v2.jsonl              # 中文维基数据（用于向量化）
└── en_wiki_v3.jsonl              # 英文维基数据（用于向量化）
```

### 数据格式说明

#### traindata格式（JSON数组）
```json
[
  {
    "text": "文本内容",
    "entities": [
      {
        "start": 0,
        "end": 5,
        "text": "实体文本",
        "type": "实体类型"
      }
    ],
    "sample_id": 1
  }
]
```

#### train.txt格式（JSONL，每行一个JSON对象）
```json
{
  "content": "文本内容",
  "result_list": [
    {
      "text": "实体文本",
      "start": 0,
      "end": 5,
      "url": "https://...",
      "prompt": "实体类型"
    }
  ],
  "prompt": "实体类型"
}
```

---

## 🎯 模型训练

### 方法1：自动化流水线（推荐）

```bash
cd work_wyy
python auto_pipeline.py
```

这会自动执行：
1. 检查前置条件
2. 训练NER模型
3. 测试模型效果
4. 提取实体词并向量化
5. 向量化并存入ES
6. 运行正式测试

### 方法2：手动分步训练

#### 步骤1：检查前置条件

```bash
cd work_wyy/ner
python check_prerequisites.py
```

检查项包括：
- ✅ 基础模型是否存在
- ✅ 训练数据是否存在
- ✅ Python依赖是否安装
- ✅ CUDA是否可用

#### 步骤2：训练模型

```bash
python finetune_ner_model.py
```

**训练配置**：
- 基础模型：`chinese-roberta-wwm-ext-large`
- 训练轮数：5 epochs
- 批次大小：8
- 学习率：2e-5
- 最大序列长度：512

**训练数据**：
- traindata目录（6组训练/验证文件）
- ccks_ner（400个JSON文件 + 5个fold的BIO数据）
- train.txt（JSONL格式）
- MSRA数据（通用NER）

**预期训练时间**：
- GPU：约30-60分钟（数据量约51,000条）
- CPU：约2-4小时

**输出**：
- 模型保存到：`work_wyy/model/ner_finetuned/`
- 标签映射：`work_wyy/model/ner_finetuned/label_mapping.json`
- 训练日志：`work_wyy/ner/ner_finetune.log`

#### 步骤3：测试模型

```bash
python diagnose_ner_model.py
```

测试内容包括：
- 加载模型和标签映射
- 测试实体提取功能
- 显示预测结果和概率

**预期结果**：
- F1-Score: 98.26%
- 能够正确识别27种军事领域实体类型

---

## 🔍 实体提取与向量化

### 提取实体词并向量化

使用NER模型从每条数据的描述中提取实体词，并用微调后的模型进行向量化：

```bash
cd work_wyy/data
python find_top_k.py
```

**输入文件**：
- `zh_wiki_v2.jsonl`（中文维基数据）
- `en_wiki_v3.jsonl`（英文维基数据）

**输出文件**：
- `entity_words_zh.jsonl`（中文实体词及向量）
- `entity_words_en.jsonl`（英文实体词及向量）

**处理流程**：
1. 读取原始数据（包含`zh_description`和`en_description`）
2. 使用NER模型提取实体词
3. 使用微调后的模型对实体词进行向量化
4. 合并多个实体词的向量（mean pooling + L2归一化）
5. 保存到JSONL文件

**日志文件**：
- `work_wyy/trainlog/extract_entity_words_*.log`
- `work_wyy/trainlog/find_top_k_console_*.log`

---

## 💾 数据入库

### 向量化并存入Elasticsearch

```bash
cd work_wyy/vector
python vector2ES.py
```

**输入文件**（自动查找）：
- `work_wyy/data/entity_words_zh.jsonl`
- `work_wyy/data/entity_words_en.jsonl`

**处理内容**：
1. 读取实体词文件
2. 生成所有向量字段：
   - `label_vector`（标签向量）
   - `label_zh_vector`（中文标签+别名向量）
   - `label_en_vector`（英文标签+别名向量）
   - `descriptions_zh_vector`（中文描述向量）
   - `descriptions_en_vector`（英文描述向量）
   - `entity_words_zh_vector`（中文实体词向量）
   - `entity_words_en_vector`（英文实体词向量）
3. 存入Elasticsearch索引`data2`

**前置检查**：
- ✅ ES连接是否正常
- ✅ 索引是否存在
- ✅ 实体词文件是否存在

**输出**：
- 向量化统计信息
- 导入进度和结果
- 向量字段存在性检查

---

## 🔎 检索系统使用

### 评估所有检索方案

```bash
cd work_wyy
python search_vllm.py
```

这会评估所有5种检索方案并生成对比报告。

### 评估单个方案

```bash
# 方案1：纯向量检索
python search_vllm.py --vector-only

# 方案2：纯ES文本搜索
python search_vllm.py --es-text-only

# 方案3：纯LLM判断
python search_vllm.py --llm-only

# 方案4：向量+LLM（始终重排序）
python search_vllm.py --vector-llm-always

# 方案5：向量+LLM（智能混合，推荐）
python search_vllm.py --vector-llm
```

### 输出结果

**控制台输出**：
- 实时进度条
- 各方案的MRR、Hit@1、Hit@5、Hit@10指标

**日志文件**：
- `trainlog/search_vllm_console_*.log`（控制台输出）
- `trainlog/vector_search_test_*.log`（详细日志）

**评估报告**：
- `trainlog/evaluation_report_*.json`（详细结果）
- `trainlog/evaluation_summary_*.json`（汇总对比）

### 5种检索方案对比

| 方案 | 模式 | 特点 | 适用场景 |
|------|------|------|----------|
| 方案1 | `vector_only` | 纯向量检索，速度快 | 快速检索，不要求高精度 |
| 方案2 | `es_text_only` | 纯文本搜索，不依赖向量 | 向量未就绪时的备选 |
| 方案3 | `llm_only` | 纯LLM语义判断 | 高精度要求，可接受较慢速度 |
| 方案4 | `vector_with_llm_always` | 始终使用LLM重排序 | 高精度要求 |
| 方案5 | `vector_with_llm` | **智能混合模式** | **推荐使用**，兼顾效率和精度 |

---

## 🚀 完整流程（自动化）

### 一键运行完整流程

```bash
cd work_wyy
python auto_pipeline.py
```

### 流水线步骤

1. **check** - 检查前置条件
   - 验证环境、数据、模型

2. **train** - 训练NER模型
   - 自动加载所有训练数据
   - 训练微调模型

3. **test** - 测试NER模型
   - 验证模型效果
   - 如果测试失败，终止流水线

4. **extract** - 提取实体词并向量化
   - 使用NER模型提取实体词
   - 使用微调模型向量化

5. **vectorize** - 向量化并存入ES
   - 生成所有向量字段
   - 存入Elasticsearch

6. **final_test** - 运行正式测试
   - 评估所有5种检索方案
   - 生成对比报告

### 灵活执行

#### 从指定阶段开始

```bash
# 从测试阶段开始（跳过训练）
python auto_pipeline.py --from test

# 从实体词提取开始
python auto_pipeline.py --from extract

# 只运行最终测试
python auto_pipeline.py --from final_test
```

#### 跳过指定阶段

```bash
# 跳过前置检查
python auto_pipeline.py --skip check

# 跳过训练
python auto_pipeline.py --skip train

# 跳过多个阶段
python auto_pipeline.py --skip train --skip test
```

#### 列出所有阶段

```bash
python auto_pipeline.py --list-steps
```

### 日志文件

所有日志保存在 `work_wyy/trainlog/` 目录：

- `auto_pipeline_console_*.log` - 流水线控制台输出
- `auto_pipeline_*.log` - 流水线详细日志
- `extract_entity_words_*.log` - 实体提取日志
- `find_top_k_console_*.log` - 实体提取控制台输出
- `search_vllm_console_*.log` - 检索系统控制台输出
- `vector_search_test_*.log` - 检索系统详细日志

---

## 📝 从头开始完整流程

### 第一次运行（完整流程）

```bash
# 1. 进入项目目录
cd work_wyy

# 2. 检查环境
cd ner
python check_prerequisites.py

# 3. 返回主目录，运行自动化流水线
cd ..
python auto_pipeline.py
```

### 后续运行（如果模型已训练）

```bash
# 如果模型已训练，可以从提取实体词开始
python auto_pipeline.py --from extract

# 或者只运行检索测试
python auto_pipeline.py --from final_test
```

### 重新训练模型

```bash
# 从训练阶段开始
python auto_pipeline.py --from train

# 或者手动训练
cd ner
python finetune_ner_model.py
```

---

## ⚠️ 常见问题

### Q1: 训练时提示"模型路径不存在"

**A**: 确保基础模型已下载到 `work_wyy/model/chinese-roberta-wwm-ext-large/`

### Q2: ES连接失败

**A**: 检查 `es_client.py` 中的连接配置，确保ES服务已启动

### Q3: 实体提取效果不好

**A**: 
- 检查训练数据质量
- 尝试增加训练轮数
- 查看 `ner/实体提取问题诊断.md`

### Q4: 向量化速度慢

**A**: 
- 确保使用GPU（CUDA）
- 检查batch_size设置
- 使用批量向量生成

### Q5: 检索结果不理想

**A**: 
- 尝试不同的检索方案（推荐方案5）
- 检查向量质量
- 查看评估报告分析问题

---

## 📚 相关文档

- [主README](./README.md) - 项目概览
- [NER训练与使用文档](./ner/训练与使用文档.md) - NER详细文档
- [检索方案对比说明](./检索方案对比说明.md) - 检索方案详解
- [NER基础文档](./ner/README.md) - NER快速入门

---

**文档版本**: v2.0  
**最后更新**: 2025-12-08

